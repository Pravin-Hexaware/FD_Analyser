Implement the canonical BSE flow: type scrip code, MUST click first autocomplete suggestion (#SearchQuotediv2 li), then set Broadcast Period to 'Beyond last 1 year', submit, wait for gvData rows, extract Std XBRL. Skipping the suggestion click yields an empty grid — that is a FAIL. An empty DEFAULT landing grid after page reload (no company search) is BSE downtime — Fetch Filings must NOT start heal; tell the user to retry later. Test company {test_input} must yield HARNESS_ROW_COUNT>0, FIELD suggestion_items click_first (or keyboard_select), HEALED_MODULE_OK + real xbrl_url.
